import '../../../shared/widgets/coming_soon_scaffold.dart';

class ProgressScreen extends ComingSoonScaffold {
  const ProgressScreen({super.key})
      : super(
          title: 'Progress',
          phaseNote:
              'Charts, PRs and streaks land in implementation phase 12, '
              'once there is real workout/weight history to plot.',
        );
}
