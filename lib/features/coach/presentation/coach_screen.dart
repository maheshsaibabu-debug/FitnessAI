import '../../../shared/widgets/coming_soon_scaffold.dart';

class CoachScreen extends ComingSoonScaffold {
  const CoachScreen({super.key})
      : super(
          title: 'Coach',
          phaseNote:
              'The AI coach (cloud + offline rule-based fallback) lands in '
              'implementation phase 11, after the deterministic engines exist '
              'for it to reason over.',
        );
}
