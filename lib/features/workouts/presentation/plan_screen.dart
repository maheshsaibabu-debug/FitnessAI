import '../../../shared/widgets/coming_soon_scaffold.dart';

class PlanScreen extends ComingSoonScaffold {
  const PlanScreen({super.key})
      : super(
          title: 'Plan',
          phaseNote:
              'Workout plan generation lands in implementation phase 7 '
              '(deterministic workout-generation engine).',
        );
}
